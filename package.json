{
  "name": "iframify",
  "version": "0.0.1",
  "description": "Wraps the given HTML code in an iframe that resizes dynamically to fit its contents.",
  "tags": ["iframe", "html", "resize", "wrap", "client side"],
  "dependencies": {
    "component-query": "0.0.3"
  }
}
